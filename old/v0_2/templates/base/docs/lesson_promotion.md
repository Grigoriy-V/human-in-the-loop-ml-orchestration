# Lesson promotion

Local issue -> adapter report -> candidate rule -> human review -> Core
candidate -> isolated bootstrap validation -> independent review. Classify
each lesson as Core, adapter-local, or private-never-promote. Private context,
secrets, data, and machine-specific paths never enter reusable templates.
